# 컴퓨터 자료구조 설명자료

## 0) 이 자료의 목표
- 자료구조의 **정의**와 **왜 필요한지**를 이해한다.
- 자주 쓰는 자료구조의 **특징, 장단점, 시간복잡도**를 비교한다.
- 문제 상황에 따라 어떤 자료구조를 선택해야 하는지 판단한다.

---

## 1) 자료구조란?
자료구조(Data Structure)는 데이터를 효율적으로 저장하고, 조회하고, 수정하기 위한 방법이다.  
같은 데이터라도 어떤 구조를 쓰느냐에 따라 프로그램 속도와 코드 복잡도가 크게 달라진다.

### 핵심 포인트
- 저장 방식이 다르면 연산 비용도 달라진다.
- 연산(삽입/삭제/탐색)의 우선순위에 따라 적합한 구조가 다르다.
- 자료구조 선택은 성능뿐 아니라 코드 가독성/유지보수성에도 영향을 준다.

---

## 2) 자료구조 선택 기준
1. 데이터에 자주 수행하는 연산이 무엇인가?
2. 데이터 크기는 어느 정도인가?
3. 중복 허용/순서 보장/정렬 상태가 필요한가?
4. 메모리 사용량 제약이 있는가?
5. 최악 시간복잡도 보장이 필요한가?

---

## 3) 선형 자료구조

## 3-1) 배열(Array)
### 개념
연속된 메모리 공간에 같은 타입의 데이터를 순서대로 저장한다.

### 특징
- 인덱스로 즉시 접근 가능
- 중간 삽입/삭제 시 데이터 이동 비용 큼

### 시간복잡도(평균)
- 접근: `O(1)`
- 탐색: `O(n)`
- 끝 삽입: `O(1)` (동적 배열 리사이즈 시 `O(n)` 발생 가능)
- 중간 삽입/삭제: `O(n)`

### 의사코드
```text
PSEUDOCODE: ARRAY_INSERT_MIDDLE(arr, index, value)
1. if index < 0 or index > length(arr): return error
2. arr의 끝에 빈 칸 하나를 확장한다
3. length(arr)-1부터 index까지 한 칸씩 오른쪽으로 이동
4. arr[index] <- value
5. return arr
```

---

## 3-2) 연결 리스트(Linked List)
### 개념
각 노드가 데이터와 다음 노드(또는 이전 노드) 주소를 가진다.

### 특징
- 중간 삽입/삭제가 포인터 변경으로 쉬움
- 인덱스 기반 임의 접근이 느림

### 시간복잡도(평균)
- 접근(인덱스): `O(n)`
- 탐색: `O(n)`
- 머리 삽입/삭제: `O(1)`
- 중간 삽입/삭제(위치 노드 알고 있을 때): `O(1)`

### 의사코드
```text
PSEUDOCODE: LINKED_LIST_INSERT_AFTER(node, value)
1. newNode를 생성하고 newNode.value <- value
2. newNode.next <- node.next
3. node.next <- newNode
4. return head
```

---

## 3-3) 스택(Stack, LIFO)
### 개념
가장 마지막에 넣은 데이터를 가장 먼저 꺼낸다(Last In, First Out).

### 사용 예
- 함수 호출 스택
- 괄호 검사
- 되돌리기(Undo)

### 시간복잡도
- push: `O(1)`
- pop: `O(1)`
- top/peek: `O(1)`

### 의사코드
```text
PSEUDOCODE: STACK_PUSH(stack, value)
1. if stack is full: return error
2. stack.top <- stack.top + 1
3. stack[stack.top] <- value
4. return success
```

```text
PSEUDOCODE: STACK_POP(stack)
1. if stack is empty: return error
2. value <- stack[stack.top]
3. stack.top <- stack.top - 1
4. return value
```

---

## 3-4) 큐(Queue, FIFO)
### 개념
먼저 들어온 데이터를 먼저 꺼낸다(First In, First Out).

### 사용 예
- 작업 스케줄링
- 프린터 대기열
- BFS(너비 우선 탐색)

### 시간복잡도
- enqueue: `O(1)`
- dequeue: `O(1)`

### 의사코드
```text
PSEUDOCODE: ENQUEUE(queue, value)
1. if queue is full: return error
2. queue.rear <- queue.rear + 1 (원형 큐는 mod 연산)
3. queue[queue.rear] <- value
4. return success
```

```text
PSEUDOCODE: DEQUEUE(queue)
1. if queue is empty: return error
2. value <- queue[queue.front]
3. queue.front <- queue.front + 1 (원형 큐는 mod 연산)
4. return value
```

---

## 3-5) 데크(Deque)
### 개념
양쪽 끝에서 삽입/삭제가 가능한 큐.

### 특징
- 스택 + 큐의 성격을 동시에 가짐
- 슬라이딩 윈도우 문제에서 자주 사용

### 시간복잡도
- 앞/뒤 삽입, 삭제: `O(1)`

---

## 4) 비선형 자료구조

## 4-1) 트리(Tree)
### 개념
계층적 관계를 표현하는 구조(루트-자식 관계).

### 용어
- 루트(Root), 부모(Parent), 자식(Child), 리프(Leaf), 높이(Height)

### 이진 탐색 트리(BST) 특징
- 왼쪽 서브트리 < 현재 노드 < 오른쪽 서브트리
- 평균 탐색/삽입/삭제 `O(log n)`
- 편향되면 최악 `O(n)`

### 의사코드(BST 탐색)
```text
PSEUDOCODE: BST_SEARCH(node, target)
1. if node is null: return null
2. if node.value == target: return node
3. if target < node.value: return BST_SEARCH(node.left, target)
4. else: return BST_SEARCH(node.right, target)
```

---

## 4-2) 힙(Heap)
### 개념
완전 이진 트리 기반 우선순위 자료구조.

### 종류
- 최대 힙: 부모 >= 자식
- 최소 힙: 부모 <= 자식

### 주요 연산 시간복잡도
- 삽입: `O(log n)`
- 최댓값/최솟값 조회: `O(1)`
- 최댓값/최솟값 삭제: `O(log n)`

### 사용 예
- 우선순위 큐
- 스케줄러
- 힙 정렬

---

## 4-3) 그래프(Graph)
### 개념
정점(Vertex)과 간선(Edge)으로 관계를 표현.

### 표현 방식
- 인접 행렬: 구현 단순, 메모리 사용 큼(`O(V^2)`)
- 인접 리스트: 희소 그래프에 효율적(`O(V + E)`)

### 대표 탐색
- DFS(깊이 우선 탐색): 스택/재귀 기반
- BFS(너비 우선 탐색): 큐 기반

### BFS 의사코드
```text
PSEUDOCODE: BFS(graph, start)
1. 방문 집합 visited를 만든다
2. 큐 queue를 만들고 start를 넣는다
3. while queue가 비어있지 않다:
   3-1. current <- queue에서 하나 꺼낸다
   3-2. if current가 이미 방문됨: continue
   3-3. current를 방문 처리한다
   3-4. current의 인접 정점 중 미방문 정점을 queue에 넣는다
4. return visited
```

---

## 5) 해시 테이블(Hash Table)
### 개념
키를 해시 함수로 변환해 버킷 위치를 찾는 구조.

### 특징
- 평균 탐색/삽입/삭제 `O(1)`
- 해시 충돌 관리 필요(체이닝, 오픈 어드레싱)
- 순서 보장이 필요한 경우 별도 구조 필요

### 사용 예
- 사전(Dictionary), 캐시, 중복 검사

### 의사코드
```text
PSEUDOCODE: HASH_PUT(table, key, value)
1. index <- hash(key) mod table.capacity
2. if table[index] 비어있음: 삽입
3. else 충돌 처리 규칙(체이닝/탐사)에 따라 저장
4. return success
```

---

## 6) 시간복잡도 빠른 비교표

| 자료구조 | 접근 | 탐색 | 삽입 | 삭제 | 비고 |
|---|---|---|---|---|---|
| 배열 | O(1) | O(n) | O(n) | O(n) | 끝 삽입은 평균 O(1) |
| 연결 리스트 | O(n) | O(n) | O(1)\* | O(1)\* | \*위치 노드를 알고 있을 때 |
| 스택 | - | - | O(1) | O(1) | LIFO |
| 큐 | - | - | O(1) | O(1) | FIFO |
| 해시 테이블 | - | O(1) 평균 | O(1) 평균 | O(1) 평균 | 충돌 시 성능 저하 |
| BST | O(log n) 평균 | O(log n) 평균 | O(log n) 평균 | O(log n) 평균 | 편향 시 O(n) |
| 힙 | - | - | O(log n) | O(log n) | top 조회 O(1) |
| 그래프(인접 리스트) | - | O(V+E) | - | - | 탐색 기준 |

---

## 7) 상황별 선택 가이드
- 빠른 인덱스 접근: **배열**
- 삽입/삭제가 잦고 위치 이동이 많음: **연결 리스트**
- 최근 작업 취소/되돌리기: **스택**
- 작업 대기열/순차 처리: **큐**
- 키-값 빠른 조회: **해시 테이블**
- 정렬된 계층 데이터 탐색: **트리(BST/균형 트리)**
- 우선순위 기반 처리: **힙(우선순위 큐)**
- 네트워크/경로/관계 표현: **그래프**

---

## 8) 간단 파이썬 구현 예시

### 스택
```python
class Stack:
    """LIFO 스택 구현."""
    def __init__(self):
        self.items = []

    def push(self, value):
        self.items.append(value)

    def pop(self):
        if not self.items:
            raise IndexError("stack is empty")
        return self.items.pop()
```

### 큐(deque 사용)
```python
from collections import deque

class Queue:
    """FIFO 큐 구현."""
    def __init__(self):
        self.items = deque()

    def enqueue(self, value):
        self.items.append(value)

    def dequeue(self):
        if not self.items:
            raise IndexError("queue is empty")
        return self.items.popleft()
```

---

## 9) 학습 체크리스트
- [ ] 배열과 연결 리스트의 삽입/삭제 비용 차이를 설명할 수 있다.
- [ ] 스택과 큐의 차이를 실제 사례로 설명할 수 있다.
- [ ] 해시 충돌 해결 방식 2가지를 말할 수 있다.
- [ ] BST가 편향될 때 왜 느려지는지 설명할 수 있다.
- [ ] BFS와 DFS를 각각 언제 쓰는지 판단할 수 있다.

---

## 10) 연습 문제
1. 괄호 문자열이 올바른지 검사하는 알고리즘을 작성하라. (힌트: 스택)
2. 정수 배열에서 중복 원소를 빠르게 찾는 방법을 작성하라. (힌트: 해시 테이블)
3. 최단 간선 수 경로를 구하는 탐색을 작성하라. (힌트: BFS)
4. 우선순위가 높은 작업부터 처리하는 스케줄러를 설계하라. (힌트: 힙)

이 자료를 기반으로, 다음 단계에서는 각 자료구조를 직접 구현하고 테스트 케이스를 작성해보면 학습 효과가 크다.
